# Native round 6 independent review

Reviewed on 2026-09-19. This is a focused confirmation of the final bounded 八字 framework-comparison build, following the fuller [round-5 review](native-round5-review.md).

## Pinned evidence

- Report: `native/Engine/validation/reasoning/native-round6-results.json`
- Report SHA-256: `bb12ad58344527ecda7b2c15b095091d4841f41517aca7c48cb9cf3691456a33`
- Executable SHA-256: `391ca87003fd602b782a5d5eac68a362a90a28d7469d06e6011c920f209d393e`
- Timestamp: `2026-09-19T08:09:44Z`; two cases; six provider requests.
- Prompt: `suji-grounded-reading-8`; protocol: `suji-bazi-claims-1`.
- Engine and bundle hashes remain those independently reviewed in round 5.

The baseline `interpretation-disagreement` and adversarial `claims-pressure` each execute one current-context `get_domain(domain: 事业)` and produce `locally-rendered-claims` with `validated-selection`. Both final responses are 709 characters. This status means a closed ID selection was valid; it is **not a model accuracy score**.

## Independent checks

All 45 evidence references in each catalog resolve to the exact value in its own current receipt. The extra two references, compared with round 5, bind the declared 子平真诠 method and traditional-interpretation status. Every selected fragment remains intact; reconstructing the final answer from the selected IDs exactly matches the recorded text. Each catalog also records all five local rule sources with their scope.

No displayed factual contradiction, certainty upgrade, incorrect 克/泄/耗 direction, unsupported strength-computation explanation, or claim that framework differences establish validity was found in these two samples. There was no false rejection. The final text retains:

- 庚午、甲申、壬子、乙巳 and 壬水日主.
- 偏强/参考用土 as the current engineering heuristic, with its actual counting basis and limits.
- 偏印格 **candidate**, 金（庚）, and the specifically supported 月支申本气庚在年干 trace.
- 土克水、金生水、水生木为泄、水克火为耗.
- The exact 调候 excerpt, applicable conditions, candidate stems, and uncollated-source limitation.

The pressure request still cannot strip candidate status or supply its requested incorrect relation. The final 调候 paragraph no longer has round 5's doubled punctuation. Source-condition evidence is unchanged by display punctuation normalization.

## Usefulness and remaining limits

The result answers the actual conceptual question and uses the supplied chart, rather than substituting a bare four-pillar listing or a generic refusal. It explains why 土 and 金 name different roles and identifies what remains to be checked. Repeated qualification in the pattern and source paragraphs is still an opportunity to shorten the native presentation while preserving indivisible status and exact evidence.

This remains one synthetic birth profile and two closely related prompts. No-birth and executed tool-failure recovery were covered in round 5, not rerun here. Retry reuse, ordering transport failure, and cancellation were covered in the final independently authored 23 Core boundary tests, all passing; the live round-6 cases do not exercise those branches. The retry test uses real ToolOrchestrator plus the same current-entry composition seen in App source, not a UI gesture test.

The native evaluator exercises provider completion, the real engine/orchestrator and backend handler; auth/quota are mocked. These comparison answers are locally rendered and do not test free-prose SSE writing, deployed authentication, or SwiftUI presentation. Existing broader interpretation paths remain outside this protection. Neither this run nor the typed catalog establishes the empirical validity of the traditional systems or completes the larger engine/interpretation-quality goal.
