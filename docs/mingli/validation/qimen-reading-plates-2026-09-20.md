# 奇门盘层陈述核验：F3

本批只接受有限的当前盘面天干／宫位肯定句核验，不代表整个解读通过。计算数据来自既有回执，不重算、不重起盘。

## 实际缺陷与修复

`core-e2a-final-results.json`被模型核验接受，却把地盘戊说在巽四宫；实际地盘戊在震三、巽四地盘为己。这份完整原始draft和writerHistory已作为永久回归：即使二次模型给accepted，也先由本地对应字段要求修正。

- 支持“地盘戊在巽宫”与“巽宫地盘为戊”这两种有限句式；普通地盘、普通天盘、地盘寄干、天禽／天盘寄干分别绑定。
- 不将寄干当作普通干错误：坤二地盘乙且寄庚时，“地盘庚在坤宫”有据；旧fixed-Kun盘可用中宫地盘佐证，缺该依据的稀疏盘不授权改写。
- 所有可用奇门回执必须对被指字段及寄干集合一致；相冲突或缺字段的其他盘不能被忽略。数组索引、宫ID及回执ID各自保持。
- 模型核验反馈沿用原值、原路径、原类型验证。问号保留至肯定句检查；引用错误字段不能授权改写。
- 独立复核发现拆分冒号／逗号会丢失“待核对”“并未证明”等语境。先写失败测试，再改为整句有限语法；任意未知前后分句使该句退出核验，不继续扩张否定词黑名单。原真实错误仍可复现并拦截。

## 真实供应商回放仍未通过广义解读验收

`core-f3-results.json`：真实Swift工具编排、JavaScriptCore、写作和核验，经后端处理器调用DeepSeek，共5次供应商请求、1份原盘回执。认证和配额仍为合成替身，不是部署后认证／SwiftUI验收。

prompt15，执行文件SHA `2371a5d41d1b03738405681cc5de33bdda45d1080b5b5b6c8a703efffba2ad91`。记录生成于语境边界增量修正前；不冒称最后执行文件的回放。

这次地盘戊3、天盘戊4、天禽寄庚7均正确，但末尾说“日干、时干同干不同宫”，混淆两种身份与各自的地盘／天盘；并建议用户自行选宫取用，缺少所选取用规则支持。“天禽在中宫”也没有清楚区分留存记录和实际寄宫。模型先给出类型不匹配的拒绝意见，重试又接受；协议accepted不算语义正确，F仍未验收。

多轮真实反例说明：补提示、二次模型检查和少量文字模板不能成为完整事实保障。后续应将关键盘面陈述由本地证据条目编译，日时身份、两层寄干、空值和未定用限制随完整条目绑定。不得用用户选宫代替流派取用，也不得把局部字面检查扩称通用判真器。

## 验证

- 5项永久回归覆盖真实错宫位、正反句式、两层寄干、旧盘、稀疏与冲突回执、问句和语境边界；已观察RED/GREEN。
- 完整Core最终268项测试通过，日志：`/tmp/suji-f3-final-core.log`。独立最终复核的11例通过，前述P2已清除；仅确认有限字面范围。native-only与diff检查通过。
- E2a远端：`49aed08b04b3c054aa6908b0800c5b23ed719ec8`，run35497388190，engine、swift-core、native-app均成功。F3自身提交与远端结果待补。
