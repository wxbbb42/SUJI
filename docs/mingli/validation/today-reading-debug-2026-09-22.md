# 问道今日问答调试（2026-09-22）

基线为 `47467d2`。本次从 iPhone 16 Plus 上「今天适合干什么」的失败会话开始，检查了实际计算回执、重试、Swift 核对流程和 App 编排。修复使用 `suji-grounded-reading-17`，保留原有计算引擎和已保存盘面。

真机记录确认该问题已有成功的 `get_today_context` 回执，包含 `todayGanZhi`；回退文案却显示空的日干支。原安装版本重试后成功返回回答。首次被拒绝的草稿和内部原因没有保存在会话中，不能据此把首次拒绝全部归因于字段错误。

另一个真机同义问答没有保存任何工具回执，回复却声称计算未完成。镜像输入还带入了一个前导退格字符；新的意图匹配处理首尾控制字符，不改聊天原文，也不删除词语中间的退格字符。

| 问题 | 修复与回归 |
| --- | --- |
| 工具返回 `todayGanZhi`，核对索引和回退读取 `dayGanZhi` | 索引指向真实字段，旧回执保留原指针；回退展示实际日干支，省略缺失的日期和空节气。用随 App 分发的真实引擎输出验证。 |
| 明确的今日问答可能跳过取数 | `TodayReadingRequest` 对一组完整、明确的中英文今日问答固定调用一次现有工具；成功缓存按原上下文复用。否定、其他日期、其他模式、指定术数和混合问题保持原路径。 |
| 自然日期句不能绑定日柱纠正 | 增加「今天是某年、某月、某日」等有限语法，要求明确的当下时间主体；「你的日柱」「后天日柱」、未标明主体的日柱、假设、否定和问句不能被当天回执反向纠正。 |
| 无效核验协议也显示“盘面不一致” | 未完成核对与已支持的拒绝分别显示；计算资料是否存在由实际回执决定。工具失败并且草稿被拒绝时，不声称已有盘面保存。App 日志只增加失败原因，不记录草稿、出生资料或凭据。 |
| 没有调用工具却声称计算失败 | 写作约束区分未计算与明确工具错误。今日问答把计算事实与一般生活建议分开；此提示改动尚未完成修复版的真实模型复测。 |

验证使用 Xcode 27.0。结果如下：

| 检查 | 结果 |
| --- | --- |
| TypeScript 命理测试 | 77 suites / 1,016 tests 通过 |
| TypeScript 类型检查 | 通过 |
| 最终 Swift Core 全量，`TZ=America/Los_Angeles` | 471 项，470 通过，1 项可选容量矩阵测试跳过，0 失败 |
| 最终 App hosted 测试 | 15 项通过：今日问答、占问确认、原盘补充与重试 |
| 本轮早期的 UI / 存储回归 | 11 项 UI 与 7 项存储回归通过；不是最终提示版本的真实模型验证 |
| Supabase 本地后端测试 | 15 项通过，含本地 PostgreSQL 配额检查 |
| 仓库检查 | SwiftUI-only 与 `git diff --check` 通过 |

App hosted 测试执行实际 `ChatSession`、本地引擎、SwiftData 与 SSE/核对流程，网络回复由 URLProtocol fixture 提供。失败后重新建立 AppStore，再禁止引擎执行工具，验证重试继续使用同一回执。它证明编排和恢复行为，不证明真实模型回答质量。

本机依赖镜像缺少锁文件中的 `is-core-module@2.17.0`，`npm ci` 因此失败。本地使用 `npm install --no-save --package-lock=false --ignore-scripts` 安装声明的直接依赖后运行测试，传递依赖由镜像解析，其中 `is-core-module` 为 2.16.2。仓库的 package.json 和 lockfile 均未修改；以上 TypeScript 结果不等于完整复现锁文件安装。

本地证据：`/tmp/suji-core-final-v2-20260922.log`、`/tmp/suji-engine-tests-final-20260922.log`、`/tmp/suji-engine-typecheck-final-20260922.log`、`/tmp/suji-backend-tests-20260922.log`、`/tmp/suji-today-session-reviewed-20260922.xcresult`。真机私人会话快照没有进入仓库。

真机复测仍待完成：调试中已连接并附着原 App，随后手机断开，当前 Mac 没有可用的开发签名证书。原 App 的公开服务配置已恢复到被 Git 忽略的本地文件中；修复版尚未安装到手机。需要重新连接手机并完成原开发团队签名，再检查原句、命盘读取、连续追问和失败重试。

本次结果覆盖今日问答链路及既有算法回归，没有开展新的传统规则正确性审计，也没有测量真实模型的整体拒绝率或预测有效性。
