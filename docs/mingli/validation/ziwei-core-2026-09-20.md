# 紫微核心补审：换日错误与宫位事实

本次是核心完善 D1，不是四体系全部完成。基线 `0c3ae1f`。

## 独立审查发现与修复

旧代码把原公历日期和晚子索引12传入 iztro。库推进安星日数，却未统一推进农历月年，且晚子时跳过闰月后半月处理。现有130项紫微相关测试仍能全绿，说明普通日测试不能覆盖边界。

现先保存原出生瞬间；23点将计算日期推进一天，再以子时0调用库。公历与农历输入走同一条路径；不重复推进。返回 `method.calculationDate` 与固定北京时间口径。2100年末出生可派生2101-01-01计算日期，外部出生范围仍1901–2100。缓存身份保持原始瞬间，打包内容哈希使旧命盘自动失效。

下列固定预期先按寅起月、子时命身同宫、五虎遁、纳音及安紫微补数法计算，再用于测试：

| 北京时间 | 有效农历 | 命身／宫干支／局／紫微 |
| --- | --- | --- |
| 2023-04-04 23:00 | 闰二15 | 卯／乙卯／水二／申 |
| 2023-04-05 23:00 | 闰二16，取三月 | 辰／丙辰／土五／酉 |
| 2023-04-06 23:00 | 闰二17，取三月 | 辰／丙辰／土五／寅 |
| 2024-04-08 23:00 | 三月初一 | 辰／戊辰／木三／辰 |
| 2024-02-09 23:00 | 甲辰正月初一 | 寅／丙寅／火六／酉 |

例如土五局十七日补三得二十，商四从寅数至巳，补数奇退三得寅。春节例还验证生年四化全体随年界变化，22:59仍为癸年。8项测试先失败，修复后通过。独立复审另检查了 natal→持久化→profile/tool 的出生身份和既有校准路径。

## 三方四正与四化来源

工具按实际地支位移+4、+8、+6查两个三合宫与对宫，完全不依赖数组顺序。原宫与关联宫均有星曜明细、亮度、身宫标记、空宫标记、生年四化对象。

1995-08-15 19:30女命：命戌空宫；关联官禄寅、财帛午、迁移辰。天机生年化禄、天梁生年化权实际位于迁移辰。返回对宫参照对象，不将这些星移动到空的命宫。非空宫不生成借星参照；稀疏旧盘不造出缺失宫位或生年干。6项新增测试先失败，后通过。

原生索引为每个实际宫位建立独立键与JSON指针，并覆盖domain嵌套结果。主星/辅星的自然语言字段矛盾须明确绑定实际宫名；不能把对宫或流年宫的文字用于改写本命宫事实。来源是返回的版本化元数据，不等于原生端独立认证书目。

## 来源校勘和纠正旧表述

本次实际获取《紫微斗数全书》卷二电子全文，SHA256 `1e255c6ae99e6f89ae124ab342554ef1347fc8e68fb8908cba7290d1a38cbb72`。仓库 `ziwei-core-sources.json` 保存读取地址、节选、字符位置与哈希；未做纸本校勘。

- 原文壬句为“壬梁紫府武宿是”，即天府化科。当前产品选择 iztro2.5.8默认左辅化科；已改掉旧测试声称十干全部与全书一致的名字，来源返回中保留差异。
- 原文闰月取次月；产品选择前15日当月、后15日次月，不能写成逐字古籍实现。
- “安大限诀”写从命前/后一宫起，能支持顺逆方向，但不能直接证明现代首限命宫、局数起岁全部规则。
- 三方四正几何引用固定版本工程算法，不拿散见“三方照临”的文字冒充完整计算条文。

旧研究里的“晚子索引12已修复”仅覆盖了普通月中。此次完整换日修正取代那一实现，但保留历史报告供审计；不能继续以其普通日测试宣称月年界无误。

## 验收与剩余事项

Engine全量41套624项通过，typecheck/build通过；生成16个Node/JSC运行时一致性fixture，新增春节与闰月晚子、本命宫工具。运行时fixture不计作独立真值。Swift全量与远端CI结果续记于计划和 `native/VERIFICATION.md`。

独立复审另发现并修复两项真实回归：旧稀疏盘缺少主星数组会抛错；四个领域查询的完整结果共74,484 UTF-8字节，超过原生60KB预算。新增缺数组反例与真实bundle→ToolOrchestrator集成测试，均观察失败后修复。

容量修复仅作用于模型传输：固定本命子树与来源在本轮已完整送达且JSON严格相同时，后续结果提供原工具ID与精确指针；完整receipt、持久化和原始证据不改，领域特有事实保持内联。引用不串链，不跨历史用户问题，不指向预算拒绝或失败结果。4个真实命盘使用29字符工具ID的四领域查询降至37,648–38,729字节，核验消息编码最大128,000字节；没有提高60KB/120K字符/262KB后端上限。重放同一问题的receipt也先投影再执行40KB预算，并通过内容重建验证保留原证据。真实重放先复现丢两条工具及证据的问题，再通过修复。

原生自然语言绑定目前只新增明确命名宫位的干支、位置、主辅星与生年四化字符串。亮度和结构化四化详情已可精确引用，但相关多段键尚不能单独授权自动改稿，留待D3/F；不能把事实索引覆盖等同全部解读语义验收。

最终Swift Core全量233项通过。独立复审用真实bundle和当前Swift源码另抽查36组命盘×四领域、29字符工具ID，最大38,729字节，全部恢复4条结果及证据；篡改结果或移除原引用源均不能恢复对应证据，未发现新的可行动回归。

大限/流年层、宫干飞化、流月、自然语言四化层级判别与完整端到端解释验收尚未在本批完成。没有给自动论命新的“专业完整度”分数，也没有开始七政四余或独立星宿实现。
