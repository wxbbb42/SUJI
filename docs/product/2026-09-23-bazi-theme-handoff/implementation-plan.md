# 八字主题 → 问道：实施与验收计划

基线检查点：`776b34f8bd515d53fd7567ebf278e29070750534`，已本地提交并报告；分支 `codex/fix-mingli-today-activities`。本阶段不推送、合并、发布或装真机。

## 本次交付边界

在现有 Profile → 我的册页的八字部分增加「表达与规则」主题，保持已有导航。完整阅读包括本盘锚点、组合层次、不能由此推出的结论、可反驳的现实观察、明确标作现代编辑建议的练习和依据。不是完整人格测量，不宣称格局救应胜负。未命中组合须真实区分；不会为每个人硬写同一故事。

实现采用本地确定报告；从主题进入原问道只带本机意图绑定，不自动发送、不替换已有草稿、不停止进行中任务。发送前核对账号、出生确认代际、完整出生指纹、bundle/内层算法、主题快照与内容版本，再真实执行 get_domain 的当前本命缓存投影。闭合主题协议由模型选择已审核解释/观察动作及有限追问，程序重建事实与限定并验证；不走 ReflectionSession，不把报告伪装成工具回执。普通问答仍走原生产核验，不因新增入口放松限制。相邻短答只在上条回答的当前身份、sourceUserID、context、真实投影和完整渲染正文一致后才授权；支持目标/方法/有限现实约束，不宣称通用自然语言理解。

## 顺序和所有权

1. 独立内容与技术基础评审（各自已落盘）。先修独立出生代际/内层版本/不可信natal注入；RED后GREEN。
2. Core主题编译器、严格证据映射、分支与真实合成样稿；闭合回答协议、静态白名单与动态/他人/事件/模糊分流。
3. 当前册页→原问道的待发送主题卡；保存草稿、返回主题、同一会话真实回执、失败重试/来源查看。新归档字段外部导入剥除信任。
4. Core、hosted存储与身份、UI大字号/深色/返回/草稿专项；实际查看模拟器截图。独立内容、技术、设计评审与整改。
5. 有上限真实在线复测、逐例结果、限制与第二个独立本地提交。

## 真实在线预算（预先约定）

最多8个合成场景、总计最多24次 Supabase→DeepSeek 请求；每次闭合主题最多2个模型尝试（无效协议仅修复一次，网络错误不自动重试）。至少覆盖主题首问、连续追问、不符合自述、资料变更、动态、他人/模糊、网络/核验失败恢复；本地身份拒绝和澄清应在调用前截断，不为凑次数强行调用。真实网络故障可由操作者在相同预算内手动重试一次。先用现有安全临时测试账号流程；清理仅精确测试账号及其配额，不触碰真实资料，不修改资源。原始响应留忽略 artifacts，仓库仅脱敏逐例结果与证据哈希。

计算、内容、核验、回放分别计；HTTP成功不算内容通过。未实际执行的故障场景标未验证，mock的负面测试仅证明本地保护，不冒充真实在线。旧32题18失败不被本轮覆盖或改判。

## 门槛与限制

出生资料变化必须使旧绑定失效且草稿仍在；账号切换隔离。内部完整事务/跨设备outbox不在此小步承诺范围。来源电子转录未校印本，不冒充权威人格科学。四余/紫微/星宿完整生活主题、通用自由命理综合、所有旧问道误拒/误接受、真机/人工VoiceOver与全32题付费复验均不是本轮已完成项。
