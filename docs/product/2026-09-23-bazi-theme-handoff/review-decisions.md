# 独立评审与采纳记录

检查点先于新代码：`776b34f8bd515d53fd7567ebf278e29070750534`。本阶段只做本地开发与提交，不推送、合并或发布。

实际独立评审者：`/root/theme_content_audit`、`/root/theme_ai_audit`、`/root/theme_design_audit`。它们均读取源码/文档，设计评审还实际读取模拟器截图。实现协作者 `/root/theme_core_impl`、`/root/theme_identity_impl`、`/root/theme_ui_impl` 不当作独立评审者。各原始评审保留在同目录，未用作者总结替换意见。

| 发现 | 决定及实际变化 | 验证范围/限制 |
| --- | --- | --- |
| 来源不足以从透藏直接推行为或人格 | 采纳。分开结构解释与现代沟通练习；删除“把透藏转成现实心理层次”的映射 | 来源hash/引文、五个真实引擎合成盘；不是预测效度验证 |
| 正文像术语与限制，生活价值弱 | 部分采纳。增加完整虚构例子与可反驳的观察；未强造人格分析 | 内容评审由首稿4/10改为通用帮助5/10；未达丰富生活报告目标 |
| 解释问题可能只收到练习，反对自述被当作普通问题 | 采纳。闭合协议按实际任务限制动作，保留必要限定；不符合自述不强行找隐藏人格 | 正反例、无效协议最多一次修复；仍是有限动作集合 |
| 真实工具投影缺出生参数 | 采纳。显式传当前出生资料，由AppStore注入可信缓存；不造回执 | hosted失败记录后修复，真实引擎复验 |
| 同值确认/出生A→B→A及晚到回答 | 采纳。独立birthRevision跨await核对，与scopeRevision、指纹、内层payload版本一起验证 | 本命、聊天、Reflection身份和归档回放专项；Reflection旧自由核验未整体重做 |
| 外部natal或内层版本可混入 | 采纳。丢弃调用方natal/astronomy，仅从可信当前档案注入；核对真实metadata版本 | 恶意注入、错版本回归 |
| 建档gate销毁ChatView导致草稿丢失 | 采纳。同账号入册后保持视图身份，状态条承接重建；账号变更仍隔离 | 真实修改合成出生资料、观察hasNatalDossier=false间隙并断言草稿保留 |
| 取消主题后从历史偷偷继承 | 采纳。UI发送显式inheritTheme=false，取消/切mode清绑定；不覆盖草稿 | hosted与UI边界；非UI仅允许相邻可信历史 |
| 个人练习中的同事/昨天被误路由 | 采纳窄匹配。明确术数/高风险优先，现实练习可留主题，动态/他人另分流 | 不宣称理解任意口语；具体例子漏匹配另有RED→GREEN |
| 新主动追问后“方法”“时间不能调整”丢上下文 | 采纳。只有当前身份匹配、相邻用户与回答关联、真实投影、完整正文/record一致才承接短答 | 方法/目标/有限约束白名单；不扩为自由命理综合；真实模型追问另做在线验证 |
| 受信历史context仅自洽、未直接对当前binding | 采纳P2。额外比较birthFingerprint、payloadRevision | 不把可解码历史当授权；外部归档剥离主题信任字段 |
| 继续问在长文末尾，大字难到达 | 采纳。摘要与常显限定后主入口，展开末尾保留第二入口 | 最终大字截图与流程结果见validation |
| 历史返回实际打开当前册页但措辞不明 | 采纳。改“查看当前册页”，常显历史依据说明 | 不是任意旧内容版本的完整文字重演器；返回不承诺像素滚动位置 |
| 自定义底栏盖住输入框和发送圆钮 | 采纳。根部稳定TabView与TabBar垂直分区，移除冲突inset；补完整目标的几何断言 | 截图暴露了isHittable测试不足；最终图另复核 |
| VoiceOver收起来源后的焦点 | 保留未决P3。现有语义与焦点只做静态审查，未实际VoiceOver验收 | 不能以颜色对比或自动点击代替读屏测试 |

无评审意见因“测试变绿”而被删除。最终可交付与未决范围以 [收尾交接入口](../HANDOFF.md)、`validation.md` 和各评审追加记录为准。本目录没有独立`delivery.md`，不得把不存在的文件作为证据。
