# B1–B5 本地报告实现：独立内容复核

2026-09-24。复核本轮 `BaziReadableModules.swift` 初版（189行）、`NatalReadingReport.swift` 的模块接入，以及 `synthetic-samples.md` 六份完整样稿（435行，17:23生成）。只读实现与样稿，没有运行测试、构建、引擎或远端 AI。后续若源码/样稿更新，应按下列验收点复核，不能把本文件当成新版本自动通过。

## 判定

**方向可接受，当前实现需修正后再验收。** 三份丁火酉月样稿的 B4 已能区分未定、可用、受阻；没有发现这些正文直接偷换为人格、财富或人生预测。B1/B2/B3/B5 都应继续按结构/方法导读计数，不是完整个人解读。

当前阻碍交付的重点是：B4 候选选择还缺独立闸门；详情声称“完整条件”但实际只给少量条目甚至空白；B4 摘要/详情重复且缺少“为什么是这个候选”的解释；一个缺失的可选结构字段仍能使全报告失败。修好这些可以交“八字导读增量”，仍不能宣称完整 B1–B5 个体报告或参考产品同等内容。

## 需要修正的发现

### P1 · 候选名/元素的核对还未绑定真正的月令选择

位置：`native/Core/Sources/SujiCore/BaziReadableModules.swift:103` 至116；后果进入154行 B4摘要、176行 B5比较及 `NatalReadingReport.swift:53` 首屏。

当前只检查 name 与十神可相配、selected 干的元素/十神相符，以及条件/依赖链内部相符。没有核对 `selectionBasis`、月令藏干序位、该干是否在相应位置透出、三合/三会的实际支及代理干，或者建禄/月刃/月劫的精确命名条件。现有两个 trace 不替代此步骤；`BaziRescueDependencies` 复核的是在给定 selectedYong 下的依赖，不是为什么选这个用神。

具体静态反例：样稿6实际是戊日申月，以月内壬中气透出为偏财候选。若把返回的 selected stem/name/relation 一致改为癸/正财格/正财，同时改两处 selectedYong，正偏财共享相同默认病点集，现有解析流程缺少“申并不藏癸、当前也无癸透出”的闸门。这里是依据代码的可达性判断，未运行篡改试验。另如把比肩的两种名字互换，`namesByRelation` 集合也不能证明当月就是禄月。

修正：为本地报告添加明确、只读的取格依据复核。可先只接纳已实现且可复核的分支；本/中/余气要检查序位、优先条件和透出位置；三合/三会要检查实际成员且不得把代表干说成透干；兜底 `jianlu-yueliu` 不能直接等于建禄；精确核对候选名称。无法复核则只隔离 B4 与相关 B5 比较。保留既有 Engine 候选边界，不把重建当前选择规则说成古籍完整成格裁定。

验收：正常样稿仍可读；删除/变更 selectionBasis、选入不在月支且未透出的干、替换不成立的建禄/月刃名后，首屏不得仍带通过口径的格局名。原始四柱导读保留。

### P2 · “完整条件”错误描述了经过截选的证据文本

位置：`BaziReadableModules.swift:155`。样稿4第275行、样稿6第417行；样稿1第58行、样稿5第346行也受影响。

`adjudicated.text` 只说明该核验器选择呈现的范围，原函数对五合 finding 最多取前两项，不穷尽全局条件、所有未决关系或所有根。当前文案却叫“完整条件与其他未决关系”。当它为空时，页面还以这个冒号结束，给用户一个像漏加载的段落。没有真正全面求解，不能通过称“完整”来补足。

修正：使用“这次已核对的其他关系”等限定标题，仅在非空且含新增信息时呈现；无结果时明确“当前已审核规则未提供进一步局部结论”，不加空标题。专业字段入口仍可提供完整原始数据，但原始数据完整不等于规则结论完整。

验收：六份样稿无空冒号、无“完整条件”声明；没有把“未列救应”改成“无救”或“无格”。

### P2 · 缺少 riZhuStructure 时仍可能把全册变成读取失败

位置：`BaziReadableModules.swift:162`、186；`NatalReadingReport.swift:46`。

`BaziStrengthTrace.make` 的 structure 是可选参考，传入 `.null` 时没有 `/evidence`，仍能核对固定计数并返回成功。随后186行却无条件 `evidence.at("/mingPan/riZhuStructure")`。若字段缺失，严格 pointer 读取抛错，外层整个 natal 报告失败；这与“进阶模块不影响基础事实”的新合同不符。此问题由源码路径确认，未运行注入测试。

修正：明确 B5 是否要求 structure。若要求，就在访问证据前进入 B5 的局部不可读状态；若不要求，只将实际参与且存在的结构证据加入列表。不要伪造空对象/零值填充。无论哪种，B1–B3 与其它体系不应因此消失。

验收：删除这一个可选字段后，固定计数要么仍以明确范围可读，要么仅 B5提示未核对；全册不抛错。

### P2 · 候选为什么出现仍未解释，样稿6会显得前后矛盾

位置：`BaziReadableModules.swift:34`、150–155。样稿6第377行写“月支本气庚金，食神”，第413行直接写“偏财格候选”，未连接中间原因。样稿4/6的 B4 除候选名和“继续核对”之外，几乎没有比原字典新增的解释。

这不是要求新增人生断语。现有字段已经可以解释：本气有没有透出、何种藏干透在何柱、当前优先规则因此选哪一项。用户不应该自己从专业表推导两章为何出现不同名字。

修正：P1闸门通过后，B4 加一条具体、简短的选择原因，例如样稿6：“申月本气庚没有透在四个天干；中气壬透在时干，因此当前规则取壬所对应的偏财作为格局候选。这里的取格与前面用本气说明月令关系，是两步不同的判断。”不使用未核对的数据生成这段。样稿5也应说清月本气未透、为何仍是该候选，不能写成壬已透干。

验收：普通读者能从每份 B4 找到格局名的实际选择原因。未命中后续局部规则的样稿仍是结构导读，但不只剩状态名。

### P2 · 结果复述过多，原因与结果没有形成清楚的两层阅读

位置：`BaziReadableModules.swift:149` 至155；样稿1第54/56/58行，样稿2第127/129/131行，样稿5第344/346行。

前三份 summary 和 explanation 开头逐字重复，后面直接拼入 trace 又复述同一结论；样稿5的有根不合去整段出现两次。用户展开“细读”后先重复读刚看过的句子，再遇到“救应竞争、藏干效力、全局成败、循环、异说”等未说明术语。新资料虽正确，可读性依然接近技术回执。

修正：默认摘要保留一条本盘结果和必要范围；展开第一段直接回答“为什么”：哪一干有根/无根，哪一条牵制影响了谁。只追加不重复的其他条目。样稿1应直接解释甲与己都保留作用、甲对己的制约尚未解决；样稿2说明甲无根而己仍有土根；样稿3说明甲有木根、己无土根，因此牵制方向不同。用已验证 evidence 做这三种解释，不用自由推演。

验收：展开后有新增的因果信息，不逐字重复摘要；同一条根/合结论不出现两遍；“可用”在附近说明是所选传统规则中的局部作用。

## 不阻塞这次限域增量、但必须如实记账的不足

- B1目前仍主要是日主/月令词义，没有采用已核对的同五行根与印支持。可以接受为基础导读，但不能把“仅换月份就不同”算作完整个体意义。
- B2六份都走“两端存在”的同一分支；样稿包没有覆盖缺端情况。当前图示仍一律画完整链，若以后遇到缺端，需让文字或图例清楚标出那一端当前盘未见，不能让图看起来是完整实际作用。
- B3分布清楚，但还没有用十神语言把 B4中的甲/己/癸角色讲通。最小改善可在 B4 首次出现时标一次“正印甲、食神己、七杀癸”；不用额外增加一长段词典。
- B5六份全部是固定计数偏弱，且与格局元素不同；没有“相同但不互证”的样稿，也没有固定计数与根印矩阵差异的展示。目前属于计数与格局术语比较，不能说已经展示了三种强弱/取用方法。
- 普通正格以外在 `category != zhengge` 时走同一“未通过核对”文本。合法专旺/从格/化气应写“当前普通报告尚未覆盖这一类”，与缺字段或不一致分开；不需要本轮新增这些解释。
- `foundation/localConditions` 的来源标题是大类，精确 source hash 和原句只在原始 geJuV2 字段内。闸门确实有固定来源检查，暂不构成虚构引用；后续专业依据层宜列所采用的具体规则与原句，而不是让用户从整段 JSON 找证据。

## 逐盘内容验收

| 样稿 | 本次读到了什么真实差异 | B1–B3/B5 | B4与概览 | 整份判定 |
|---|---|---|---|---|
| 1 · 1984-09-10 | 甲与己有根，己制癸仍有未解制约 | 结构/方法导读 | 状态变化正确；原因藏得较深且重复 | 有局部条件解释的导读，待修 |
| 2 · 1984-09-20 | 年甲受合牵制后，时己制月癸局部可用 | 结构/方法导读 | 所选局部效果与既有规则一致；不是人生结论 | 有局部条件解释的导读，待修 |
| 3 · 1984-09-30 | 己自身受合牵制，制癸路径受阻 | 结构/方法导读 | 有效改变主句；不能写作全格失败 | 有局部条件解释的导读，待修 |
| 4 · 1988-04-09 | 戊偏财、丙食神、丁伤官的分布；目前无透干救应候选 | 结构/方法导读 | 缺选格原因；空“完整条件”尾句 | 结构导读，未达局部个体解释 |
| 5 · 2001-11-22 | 丙辛两者有根，不能因合把一方删掉 | 结构/方法导读 | 详情有明确限制；概览只剩候选状态，且详情重段 | 有局部条件解释的导读，待修 |
| 6 · 1995-08-15 | 本气食神与中气偏财选格的区别本应可解释 | 结构/方法导读 | 当前原因缺失，只有候选/无扫描路径状态 | 结构导读，未达局部个体解释 |

“有局部条件解释”只描述内容中存在来源允许的结构意义，不是原计划的整份“个体阅读合格”，更不是预测准确率。六份全部仍非完整个体综合报告。

## 已成立的实现优点与边界

本地值投影没有合成 ToolReceipt/callID，保留原 `/mingPan` 证据；B4非通过时没有继续给 B5一个已核对的格局元素。现有 source/hash、柱位、根与 dependency 重建仍在执行路径上。所有样稿的必要限制均作为 boundary 保留，代码在默认收起状态仍显示它。没有评分、幸运处方或人格标签。保留这些约束即可，不必为了可读性放松证据条件。

本评审对新 native TabView、实际屏幕布局、VoiceOver、模型链路及当前测试结果没有独立验证，不作通过声明。

## 修正后复查 · 2026-09-24 17:28 样稿版

本次复读更新后的 `BaziReadableModules.swift`（227行）及17:28重新生成的六份样稿，并读取父任务提供的 `native/artifacts/native-report-2026-09-24/reviewer-green.log`。该日志记录 Selected tests 33项、0失败；这是对既有执行记录的读取，本评审没有自行运行测试或构建，也不将测试通过等同内容合格。

### 已关闭的发现

- **P1 候选选择闸门已补。** `selectionReason`（162行）从已验证四柱重建当前引擎的月藏实透优先链，按所选专气支政策限制可选藏干；兜底前排除月支参与的齐全三合/三会；核对实际 basis/selected stem；按精确禄月、阳刃月及月本气关系核对名字。三合/三会代理候选目前拒绝解释，没有假称实际透干。此关闭仅指当前普通正格的已接纳分支，不扩大为完整古籍取格裁定。
- **P2 缺可选结构字段导致全册失败已补。** B5在201行先要求 structure 非空，否则只返回本模块待核对内容，不再访问缺失 pointer。新增测试源码明确检查八字其它模块与紫微保留。
- **P2 “完整条件”及空尾句已删除。** 新正文不再对截选证据作完整性声明，也不再留下空冒号。
- **P2 取格原因缺失已补。** 样稿4说明辰中本气戊见年干；样稿6第415行说明申中气壬见时干、庚本气未透，因此前文食神关系与此处偏财候选不同。样稿1–3/5对本气回退也给出明确口径，未把 `jianlu-yueliu` 字符串误当成建禄。
- 摘要与展开首段的逐字复述已删除。展开首先解释选择原因，阅读层次优于初版。

### 仍需补回的一项内容：结果保留了，决定结果的具体原因被删掉

**P2，位置为 `BaziReadableModules.swift:109`–110、145–152。** 当前验证 `BaziAdjudicationTrace` 成功后完全丢弃它的内容。避免重复本身正确，但同时删除了用户理解局部差异所需的根/合事实。

17:28版样稿1–3的展开内容都是同一段酉中本气回退，再加“根气、位置和牵制不同，局部解释也会改变”的通用说明。用户看到“未定/可用/受阻”不同，却仍不知道**本盘具体哪一项不同**。原来样稿5“两方有根，不能据合删除”的确定限制也全部消失，现已降为取格导读。需要更新逐盘账本：此版样稿5不再计为有局部条件解释。

最小修复不要求新规则：在结果之外保留一次已核对的具体原因即可。例如样稿1说明甲与己都有同五行根、甲对己的制约未解；样稿2说明甲无木根而己仍有土根，甲被合牵制；样稿3说明甲有木根而己无土根，己受合牵制。可从已验证 action 的依赖对象与原四支藏干生成，或保留已有核验器中相关的根/合 finding；不要重复完整状态段，也不使用“完整条件”标题。样稿5若本轮不恢复该局部限制，则诚实按结构导读保留即可。

**截至这个版本的最终判定：证据闸门与取格说明修正通过；“为什么局部结果不同”的易读原因仍待补。** 可以如实称为“结构导读与部分局部结果增量”，尚不能声称已经把局部条件链解释完整。其它已记录的覆盖不足继续有效，未发现新增人格、财富、职业预测或评分。

## 最终复查 · 2026-09-24 17:33 样稿版

本节基于再次读取的 `BaziReadableModules.swift` 与17:33重新生成的六份 `synthetic-samples.md`，取代上一节对仍未关闭 P2 的处置结论。另已读取 `native/artifacts/native-report-2026-09-24/report-final-core.log`：日志记录 NatalReadingReportTests 14项、0失败，Selected tests 共33项、0失败，完成时间17:33:11。以上仅为父任务既有执行记录，本评审未运行测试、构建、引擎或 UI 验证。

**P2「结果保留了，具体原因被删掉」已关闭。** `BaziReadableModules.swift:152`–153现在保留一次 `adjudicated.text`，并且仅在非空时添加“已核对的部分条件”标题。选择原因仍先于局部条件，展开首段没有恢复摘要的逐字复述；标题没有把截选 trace 声称为完整条件。

- 样稿1第60–61行重新说明甲、己都有根，不能因五合认定任一方已合去；己制癸仍有未解制约。
- 样稿2第136–137行说明所选徐注条件下无根的甲受合牵制，而己有根，因而己制癸的局部路径可用。
- 样稿3第212–213行说明甲有根、无根的己受合牵制，因而己制癸的局部救应路径受阻。
- 样稿5第357–358行恢复丙、辛两方有根、不能据五合认定已合去的限制，且该段仅出现一次。
- 样稿4、6没有新增空条件标题；样稿6第427行仍明确解释申中气壬透时干、本气庚未透，连接前文食神关系与此处偏财候选。

此前关闭的候选选择闸门、可选 `riZhuStructure` 缺失时的模块隔离、部分条件口径与选择原因问题，在最终源码中均保持修正。没有为恢复解释放松来源或局部规则的核对条件，也没有伪造工具回执。

**内容评审通过，适用范围为“八字结构与局部配合增量”。** 样稿1–3及5可计为“有局部条件解释的导读”；样稿4、6仍为结构与取格原因导读。六份均不能升级表述为完整个体综合报告、经验有效性验证或竞品内容对齐完成。

前文非阻塞覆盖限制继续有效：B1主要为事实与定义；B2样稿未覆盖缺端分支；B3为结构层次而非人格解释；B5尚未覆盖同元素结果及根印矩阵比较；特殊格局与三合/三会代理仍在当前可解释范围外。术语密度与不支持分支的提示区分可在后续编辑中改善。最终未留下阻塞本次限域内容增量的发现；本结论不涉及原生导航、实际界面、无障碍或完整产品验收。
