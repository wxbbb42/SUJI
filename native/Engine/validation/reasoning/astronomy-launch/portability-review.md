# Fixture portability review

Reviewed only `check-engine-fixtures.mjs`, its six tests and the CI resource-check change. Read the failed Linux diff: the only recorded resource differences are two astronomy angle last bits; bundle/notices remain strict. Ran the six comparator tests:6 pass. No implementation edits.

**No overly permissive comparison found.** Tolerance requires an expected astronomy command/tool, an enumerated coordinate path, two finite numbers and absolute difference≤1e-9°. All object keys, array order/count, births, time, method/source policies, membership identities and nonastronomy fields remain semantically exact. The comparison does not round or modify production/cache data. Bundle/notices retain byte-level `git diff` enforcement. Allowing JSON formatting/object-key-order variation in fixtures is harmless to their content contract.

**[P2, closed] Actual cached-tool response wrapper was missing from the allowlist.** A real `get_natal_astronomy` fixture has `{request, result:{result:<astronomy>, evidence}}`; its response angle path starts `result.result.sevenBodies...`, while the initial regex only accepted `result.sevenBodies...` and `request.astronomy...`. Independently loaded the actual committed cached-tool fixture, changed only its response latitude by1e-12°, and observed comparison failure. The initial six tests missed this because the cached-tool test mutated only its sidecar and used a bare response.

Final fix independently re-read: natal commands allow only `result` coordinate paths; the explicitly named astronomy tool allows only `result.result` and `request.astronomy` coordinate paths. The updated test uses the actual response wrapper, perturbs both cache and response angles, and verifies changed evidence is rejected. Reran all six tests successfully and the comparator against all25 real fixtures successfully. **Finding closed; no remaining scoped review blocker.** Production math/data are unchanged. This is local portability-comparator approval, not a claim of remote CI success. Review stopped after this closure.
