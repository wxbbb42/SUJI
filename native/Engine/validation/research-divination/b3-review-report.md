B3 independent bounded implementation review — 2026-09-20

No blocking implementation finding within the candidate-relative original-line role scope reviewed here. The base is 966c9f5550dc1fb5c2e58fc7c26d941d6bcd1b71; reviewed file SHA-256 values are in reviewed-manifest.json. No production/shared file was changed by this reviewer.

The independent engine probe used a handwritten literal five-element role table, not the production relation helpers or the newly added repository oracle. It checked 40,960 casts (all 4,096 line-value arrays × 10 question/subject combinations), 32,768 groups, 43,136 line/hidden candidate references, 1,024 explicitly unsupported calendar references, and 115,200 actor memberships. All 10,688 忌→元 and 11,040 仇→忌 moving pairs matched the complete Cartesian pairs. This includes 5,824 groups with no 元 actor but an actual 仇 actor, and 10,368 groups with multiple candidate references. There were zero mismatches.

The final recorded Swift run rendered 1,280 actual-bundle receipts (64 original hexagrams × 10 question/subject combinations × all-moving/all-static), plus five independently specified classical-coordinate source cases and one actual-bundle calendar-candidate case. It checked 617,780 evidence pointer/value/call-ID triples and 1,348 candidate sections. All 640 all-moving reports omitted 暗动 and 静爻日冲效力; the latter appeared in role prose only when an actual static original line had a day clash. All 26 adversarial role/source/candidate mutations were rejected. Historical five-source receipts remained readable. The first attempted 5,376-record run reached source-case output, then the review harness failed while assuming this fixed date had a calendar candidate; it produced no final report. The corrected, bounded 1,280-record run is the native count relied on here.

The five handwritten cases were 乾→小畜, 大过→鼎, 兑→解, static 姤 with hidden 寅 target, and static 乾. Native text retains separate 辰/戌 targets, 午→未六合 without deciding binding, original 卯空/月破, 飞生伏 without declaring emergence, and 大过亥 target's month clash/day control together with the 忌→元 chain and explicit surviving 忌克用. Changed sixth-line 巳 is not promoted to a cross-position 仇 actor. Static 乾 is not labeled 伏吟. These source coordinates are synthetic calendar inputs for rule inspection, not asserted ancient Gregorian dates. Raw chapter 9/10 bodies match the independent prior downloads and hashes; modern appended 原劍按 is not adopted.

Projection/replay checked 10 actual chart samples with long questions, 60 positive/negative delivery-authentication cases, 10 exact replays, and 599/600/601-byte boundaries. Restoring only the question from its own call arguments reconstructs the entire original JSON. Missing calls, wrong IDs, wrong names, different question text, and duplicate matching calls cannot authenticate the projected result. An independent orchestrator probe made two requests with deliberate second-call question drift: execution/persistence happened once, both complete receipts retained original arguments/output, and each projection referenced its own call ID. A replayed third request caused zero new casts and retained its fact evidence. Those two projected messages were 50,238 bytes combined, below 60,000. This is a bounded transport check; parent-owned full provider/capacity runs are separate evidence.

This review does not establish final 用神 choice, actor effectiveness, target viability, 合住, 墓绝, emergence, net force, event outcome, dates, complete Liuyao classical coverage, or core-wide acceptance. The electronic transcription remains uncollated against print editions. Review expectations only cover the stated structural candidate layer.

Reproduce: extract the archive into /tmp, then run `/tmp/suji-b3-implementation-review/rerun.sh /path/to/SUJI`. It requires Node with the repository's TypeScript dependency, Swift/macOS, and rg. The script regenerates the large fixture file; generated 5,376-row data and compiled executables are deliberately omitted from the small archive. Source inputs, scripts, all final JSON reports, the five source-case texts and reviewed-file hashes are included.
